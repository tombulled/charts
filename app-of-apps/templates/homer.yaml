apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homer
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://github.com/tombulled/charts
      path: common
      targetRevision: feature/homer-operator # HEAD
      helm:
        valueFiles:
          - $values/homer/values.yaml
    - repoURL: https://github.com/tombulled/values
      targetRevision: feature/homer-operator # HEAD
      ref: values
  destination:
    name: in-cluster
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
