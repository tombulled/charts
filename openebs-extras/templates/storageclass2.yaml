{{- range $name, $value := .Values.storageClasses -}}
{{- include "common.storageclass" (dict
    "$" $
    "name" "bob"
    "labels" (dict "foo" "bar")
    "annotations" (dict "foo" "bar")
    "allowVolumeExpansion" true
    "mountOptions" (list "foo" "bar")
    "parameters" (dict "name" "sally" "age" 10)
    "provisioner" "foo"
    "reclaimPolicy" "Delete"
    "volumeBindingMode" "Immediate"
) -}}
{{- end }}